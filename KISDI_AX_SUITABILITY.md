# KISDI 2022–2024 지능정보사회 이용자 패널: AX 연구 적합성 감사

감사일: 2026-09-06. 범위: 사용자가 제공한 3개 ZIP의 6개 SAV와 같은 연도 폴더의 조사표·이용자 가이드북. 회귀 추정, WPS/HCCP와의 결합, 대시보드 편입은 수행하지 않았다. 개인 식별자·개별 응답은 보고서나 집계 파일에 공개하지 않았다.

## 결론

**개인의 AI 이용 경험·수용·디지털 역량을 살펴보는 별도 패널로는 활용 가능하지만, 공공기관과 민간기업의 조직관리·AX 차이를 직접 검증하는 자료로는 부적합하다.**

관측 단위는 사업체가 아니라 개인이다. 제공된 전체 변수·값 라벨에서 응답자의 직장 소유형태나 공공/민간 소속 변수를 확인하지 못했다. `DQ1`은 직업 대분류이며 관리자·전문가·사무직 등을 어느 부문으로 분류할 근거가 없다. 공무원 별도 범주도 없고, 직업군인 범주만으로 전체 공공부문을 구성해서는 안 된다.

‘정부 또는 공공기관’ 문항은 **개인정보를 관리하는 기관에 대한 응답자의 평가**이지 응답자의 소속이 아니다. 동일 응답자가 공공기관과 민간 서비스 제공자를 평가한 차이는 별도 연구 질문으로 가능하지만, 이를 공공 종사자 대 민간 종사자 비교로 표시하면 안 된다.

## 1. 파일·표본·가중치

| 연도 | 실제 응답자 N | 변수 수 | ID 변수 | wgt_a 합계 | wgt_b 합계 |
|---|---:|---:|---|---:|---:|
| 2022 | 5,378 | 404 | `id` | 39,221,658 | 5,378 |
| 2023 | 4,581 | 305 | `ID` | 39,027,595 | 4,581 |
| 2024 | 4,420 | 305 | `id` | 39,330,332 | 4,420 |

각 ZIP의 `_wgt_a).sav`와 `_wgt_b).sav`는 열과 모든 관측값이 동일하다. 두 파일 모두 `wgt_a`, `wgt_b` 열을 포함하며 SPSS의 활성 가중치 지정만 다르다. **둘을 쌓으면 표본을 이중 계산한다.** 감사에서는 연도당 a 파일 하나만 분석했다.

- `wgt_a`는 모수 규모, `wgt_b`는 표본수 규모로 정규화된 가중치이다. 연도 내 두 열은 상수배 관계여서 동일 표본의 가중 평균·비율은 같고, 가중 합계만 달라진다.
- 모든 연도의 두 가중치는 전체 응답자에게 결측 없이 양수이다. 가중 합계를 실제 N으로 보고하지 않는다.
- 2023 `wgt_a`의 최소 921.066, 최대 98,834.059, 합계 39,027,595는 가이드의 **횡단면 가중치 표 5**와 일치한다. 종단면 표 10의 최소 910, 최대 96,569와 다르다.
- 2024 `wgt_a`의 최소 1,504.140, 최대 45,880.518, 합계 39,330,332는 **횡단면 가중치 표 9**와 일치한다. 가이드의 종단면 가중치는 3개년 모두 응답한 3,895명을 대상으로 별도 산출한다.
- 따라서 제공된 a/b를 ‘횡단/종단’ 가중치 한 쌍으로 해석하면 안 된다. 제공 SAV에는 별도 종단 가중치 열이 없다. 모집단 수준의 종단 분석에는 적절한 종단 가중치 확보·검증이 선행되어야 한다. 단순 가중 회귀도 복합표본 설계에 맞춘 분산 추정을 자동 제공하지 않는다.

최초 표본은 2022년 기준 만 15–69세 중 스마트폰을 보유하고 인터넷을 하루 한 번 이상 사용하는 개인이다. 이후 해당 2022년 패널을 추적한다. 인터넷 미이용자를 포함한 모든 국민이나 전체 근로자 모집단으로 일반화할 수 없다. 실제 조사 기간은 2022년 10월 첫째 주–2023년 1월 둘째 주, 2023년 10월 첫째 주–12월 첫째 주, 2024년 9월 둘째 주–11월 셋째 주이다. 명목 연도와 실제 측정일의 차이를 유의해야 한다.

## 2. 실제 패널 연결과 품질 점검

연도별 ID는 전부 고유하며 결측·중복은 0건이다. 가이드의 동일 패널 추적 설명과 다음 ID 연결 결과가 일치한다.

| 연결 | 공통 응답자 | 앞 연도에만 존재 | 뒤 연도에만 존재 |
|---|---:|---:|---:|
| 2022→2023 | 4,581 | 797 | 0 |
| 2023→2024 | 3,895 | 686 | 525 |
| 2022→2024 | 4,420 | 958 | 0 |

3개년 공통 응답자는 3,895명이다. 2024년에만 있는 것으로 보이는 525명은 2022년에는 있었으나 2023년에 응답하지 않은 복귀 응답자이다. 신규 표본으로 간주하지 않는다.

2022 파일 안의 성별 `Sex`와 `SQ1`은 164명에서 다르다. 2022 `Sex`를 2023·2024 `SQ1`과 비교하면 각 연결 표본에서 불일치가 0건이고, 2023·2024 `SQ1`도 전원 일치한다. 반면 2022 `SQ1`을 사용하면 연결 연도별 149명·139명에서 불일치가 발생한다. **원자료를 임의 수정하지 않았으며, 성별 통제변수의 조화 규칙은 공급기관 확인 또는 명시적인 품질관리 근거가 필요하다.** 이 문제를 개인의 실제 성별 변화로 해석하지 않는다.

## 3. 주요 문항과 연속성

| 개념 | 2022 | 2023 | 2024 | 활용·주의 |
|---|---|---|---|---|
| 생성형 AI 이용 경험 | 직접 대응 문항 없음 | `q23`, 예 543/4,581 | `Q24`, 예 1,096/4,420 | 2023–2024 원문 모두 이용한 경험의 유무. 현재 조직 도입이나 최초 도입 시점이 아님 |
| 업무 관련 지능형 서비스 이용 빈도 | `Q5_19`: AI 프로그램 기반 업무 보조 서비스 | `q1_9`: 지능형 서비스의 업무 영역 | `Q1_9`: 지능형 서비스의 업무 영역 | 2022와 이후 연도의 서비스 범위·응답척도가 달라 단일 동일 문항으로 3년 결합 불가 |
| AI 수용 의도 | `Q1_1`: AI 알고리즘, `Q2_12`: AI 업무 보조 서비스 | 개별 문항 대응 검토 필요 | 개별 문항 대응 검토 필요 | 2022 의도는 생성형 AI 이용 경험이 아님. 의도 문항의 9는 모름 |
| 일상적 업무 도움 인식 | 동일 생성형 AI 문항 없음 | `q29_2`, 유효 N=543 | `Q28_2`, 유효 N=1,096 | 이용자에게만 질문. 1–5점, 객관적 생산성 아님. 2024 설명 예시 추가 |
| 아이디어 발전 도움 인식 | 동일 생성형 AI 문항 없음 | `q29_4`, 유효 N=543 | `Q28_4`, 유효 N=1,096 | 이용자에게만 질문. 개인의 지각된 유용성으로 기술, 조직 혁신 성과로 대체 금지 |
| 공공기관 개인정보 관리 평가 | `Q21_5` | `q20_5` | `q20_5` | 평가 대상이 공공기관인 문항. 소속 아님. 2024에 모름 9 추가 |
| 직업 | `DQ1` | `DQ1` | `DQ1` | 공공/민간 식별 불가. 2022–2023 자영업자 21과 2024 기타 15도 자동 대응 금지 |

업무 서비스 이용 빈도는 2022에 1–4와 9(전혀 사용하지 않음), 2023–2024에는 1–4·5(최근 1년 이용 없음)·9(전혀 이용한 적 없음)이다. 숫자 9를 일괄 결측 처리하거나 연속 점수 9로 넣어서는 안 된다. AI 수용 의도나 2024 기관 평가에서는 같은 숫자 9가 ‘모름’이므로 문항별로 처리해야 한다.

생성형 AI 이용 이유의 비이용자 결측은 구조적 비대상이다. 2023 4,038명, 2024 3,324명을 도움 인식 0점으로 채우면 안 된다. 일부 다른 하위 문항에는 서비스 유형별 추가 분기가 있으므로 척도 전체를 만들기 전 각 문항의 질문 대상을 검증해야 한다. 디지털 역량 문항도 연도별 표현·구성이 달라 같은 문항 번호만으로 동일 척도를 가정할 수 없다.

### 2023–2024 생성형 AI 경험 응답 전이

공통 3,895명에서 양 연도 경험 문항 결측은 0건이다. 아래는 비가중 응답자 수이며 추정치가 아니다.

| 2023 경험 | 2024 경험 있음 | 2024 경험 없음 |
|---|---:|---:|
| 있음 | 468 | 0 |
| 없음 | 546 | 2,881 |

경험 있음→없음 역전은 없었다. 2023 비경험자 중 연결된 3,427명을 대상으로 2024 ‘경험 있음으로의 응답 전환’을 보는 설계는 가능하다. 다만 정확한 최초 이용 날짜, 직장 사용 여부, 조직 도입 사실을 확인한 것은 아니므로 ‘조직의 신규 AX 도입’으로 부르면 안 된다.

## 4. 가능한 분석과 불가능한 주장

가능한 별도 연구 방향은 다음과 같다. 문헌 검토와 사전 분석계획 없이 현재 감사에서 회귀를 실행하지 않았다.

1. **개인 AI 수용·사용의 기술 통계:** 연도별 전체 이용 경험, 업무 서비스 이용 빈도, 이용자 대상 도움·위험 인식. 분모·응답 분기·횡단 가중치를 각각 표시한다.
2. **전향적 개인 패널 분석:** 동일인 2023 비경험자 중 2024 경험 보고 여부와 이전 디지털 역량·태도 등의 관련성. 사전 문헌에 근거한 설명변수·통제변수 선정, 측정 동등성, 탈락 편향, 종단 가중치 확보가 필요하다. 시차만으로 인과효과가 식별되는 것은 아니다.
3. **공공·민간 서비스 제공자에 대한 시민 평가 비교:** 동일 응답자의 개인정보 관리 평가 등을 비교할 수 있다. 이는 기관에 대한 신뢰·평가의 차이이며 공공/민간 조직관리 역량 차이를 직접 측정한 분석은 아니다.

이 자료만으로 불가능한 것은 공공/민간 소속별 AX 도입 회귀, 기관의 HR 정책→AX 효과, 조직의 혁신 실현·객관적 생산성 효과, WPS 사업체와의 ID 결합이다. 별도 표본의 개인 응답을 사업체 응답처럼 쌓거나, 직업을 근거 없이 소속 부문으로 치환하지 않는다. 고정효과·통제변수를 추가하더라도 이 측정 공백과 인과 식별 문제는 해결되지 않는다.

대시보드에 추가한다면 **‘개인 AI 이용 패널—조직 비교와 별도’** 영역이 적절하다. 현재는 적합성 감사만 완료했으며 실제 분포 화면의 추가 여부는 별도 결정 대상으로 남긴다.

## 5. 재현·근거

안전하게 명시된 SAV 엔트리 두 개만 연도별 고정 파일명으로 `.tmp/kisdi_ax_audit/` 안에 추출했다. 원본 ZIP/PDF는 수정하지 않았다. `archives.json`에 엔트리·크기·SHA-256을 기록했다. `metadata_*.json`은 전체 변수 라벨·응답코드, `aggregate_audit.json`은 익명 집계, `pdf_*_guide.json`/`pdf_*_survey.json`은 필요한 PDF 페이지 원문을 보관한다.

실행 명령:

```powershell
python .tmp/kisdi_ax_audit/audit_kisdi.py metadata
python .tmp/kisdi_ax_audit/audit_kisdi.py aggregates
python .tmp/kisdi_ax_audit/audit_kisdi.py guides
```

SPSS 읽기 런타임은 `.tmp/spss_reader`에 별도 설치한 pyreadstat 1.3.6/narwhals 2.25.0을 사용했다. 기존 numpy/pandas는 변경하지 않았다.

원문 근거는 사용자가 제공한 다음 파일이다. 페이지는 PDF의 물리적 페이지 번호이다.

- 「2022년 지능정보사회 이용자 패널조사 이용자 가이드북」: 6쪽(대상·표본·조사 기간), 9쪽(모집단).
- 「2023년 지능정보사회 이용자 패널조사 이용자 가이드북」: 5–7쪽(동일 패널 추적·대상·조사 항목), 12쪽 표 5(횡단 가중치), 17쪽 표 10(종단 가중치).
- 「2024년 지능정보사회 이용자 패널조사 이용자 가이드북」: 9–10쪽(대상·기간·항목), 16쪽 표 9(횡단 가중치), 18–19쪽(3개년 공통 응답자와 종단 가중치).
- 「2023년 지능정보사회 이용자 패널조사 설문지」: 17쪽 Q23(생성형 AI 경험과 분기).
- 「2024년 지능정보사회 이용자 패널조사 조사표」: 21쪽 Q24–Q28(경험·이용 유형·도움 인식·분기).

제공 원문은 재배포하지 않으며 개인 보관 경로도 공개하지 않는다. 자료의 공식 출처는 [README의 자료별 출처표](README.md#사용-데이터와-출처)를 참조한다.

가이드의 반올림 수치와 원자료 계산값은 구분했다. 회귀계수·인과효과를 산출하거나 만들어 넣지 않았다.
